sudo: required

language: java

services: 

dist:
  - xenial

jdk:
  - oraclejdk8
  
before_install:
  - chmod +x pom.xml
  
script:
  - sudo add-apt-repository -y ppa:webupd8team/java
  - sudo apt-get update
  - sudo apt-get install -y oracle-java8-installer || true
  - mvn test
